# Azure App Service configuration for Python Flask
# This file helps configure the deployment to Azure App Service

language: Python
version: 3.10
startup: gunicorn --workers 2 --worker-class sync --bind 0.0.0.0:8000 app:app
